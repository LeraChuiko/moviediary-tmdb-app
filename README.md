/*Давай перейдём в твой файл main.js.
Твоя следующая большая цель — настроить fetch-запрос к TMDB API,
чтобы вместо нашей заглушки на страницу прилетели 20 настоящих, живых фильмов.

Для этого тебе понадобятся две вещи: API Key (ключ доступа) и Base URL от TMDB.
У тебя они уже сохранены или нужно помочь найти правильную ссылку
для запроса популярных фильмов (popular movies)?*/

// 🕵️‍♂️ Шаг 1: Авторизация (Как получить пропуск на сервер?)
// Прежде чем сервер отдаст тебе хоть какие-то фильмы, он должен понять, кто ты.
// Для этого нужен специальный ключ.

// Где искать на сайте: Посмотри на левое меню документации TMDB.
// Там обычно есть разделы, связанные с началом работы
// (Getting Started или Authentication).

// Твоя задача: Найди, где описывается, как создать аккаунт и получить API Key
// (или Bearer Token / Access Token).

// Подсказка: Для отправки запросов через современный fetch сейчас очень популярен
// Bearer Token (длинный код авторизации). Найди в документации,
// куда именно его нужно вставлять в коде — в сам адрес строки (URL)
// или в заголовки запроса (headers)?

// 🎬 Шаг 2: Поиск правильного адреса (Где лежат популярные фильмы?)
// Когда пропуск (ключ) у тебя в кармане, нужно понять, по какому именно
// "адресу" (эндпоинту) отправлять запрос. В TMDB сотни эндпоинтов:
// для поиска, для актеров, для сериалов.

// Где искать на сайте: В левом меню найди большой блок, посвященный фильмам.
// Он обычно называется Movies.

// Твоя задача: Внутри раздела Movies найди конкретный пункт, который
// отвечает за Популярные фильмы (на английском это звучит как Get Popular).

// Подсказка: Обрати внимание на метод запроса. Напротив нужного адреса
// будет стоять пометка: это GET-запрос или POST-запрос? И как выглядит
// хвостик ссылки (например, что-то вроде /movie/popular)?

// 🖼️ Шаг 3: Разгадка картинок (Как собрать ссылку на постер?)
// Когда ты найдешь нужный эндпоинт, документация покажет тебе пример
// ответа от сервера (Response). И вот тут тебя будет ждать сюрприз, с
// которым сталкиваются все новички.

// В объекте фильма ты увидишь свойство poster_path, а внутри него значение
// вроде: "/1E5baAaE4ZBSnnjYRXm4RVSNczz.jpg".

// Если ты просто вставишь этот хвостик в тег <img>, картинка не отобразится.
// Сервер TMDB не хранит полные ссылки в объекте фильма, чтобы не раздувать
// размер данных. Полную ссылку нужно собрать как конструктор из трёх частей:
// Базовый URL картинок + Размер + Хвостик из объекта.

// Где искать на сайте: Ищи раздел, посвященный изображениям.
// Обычно он называется Configuration (Конфигурация) или отдельный гайд Images.

// Твоя задача: Найди, какой базовый URL используется для картинок (
// он начинается на [https://image.tmdb.org/](https://image.tmdb.org/)...)
// и какие стандартные размеры (например, w500) рекомендует документация для постеров.
